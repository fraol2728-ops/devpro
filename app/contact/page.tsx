'use client'
import SectionTitle from '@/components/ui/SectionTitle';import { useForm } from 'react-hook-form';
type F={name:string,email:string,subject:string,message:string}
export default function Contact(){const {register,handleSubmit,reset,formState:{isSubmitSuccessful}}=useForm<F>();return <section><SectionTitle title="LET'S WORK TOGETHER"/><div className='grid md:grid-cols-2 gap-8'><div className='space-y-3'><p>📧 devfraol@gmail.com</p><p>📍 Addis Ababa, Ethiopia</p><p>💼 Available for freelance</p></div><form onSubmit={handleSubmit(()=>reset())} className='glass rounded-2xl p-6 space-y-3'>{['name','email','subject'].map((f)=><input key={f} {...register(f as keyof F,{required:true})} placeholder={f==='name'?'Full Name':f[0].toUpperCase()+f.slice(1)} className='w-full bg-transparent border border-secondary/40 p-3 rounded'/>) }<textarea {...register('message',{required:true})} placeholder='Message' className='w-full min-h-32 bg-transparent border border-secondary/40 p-3 rounded'/><button className='px-6 py-3 rounded bg-primary text-white'>SEND MESSAGE</button>{isSubmitSuccessful&&<p className='text-secondary'>✓ Message Sent!</p>}</form></div></section>}
